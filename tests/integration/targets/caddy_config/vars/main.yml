---
expected_config:
  apps:
    http:
      servers:
        example:
          listen:
            - ":3456"
          routes:
            - handle:
                - handler: "static_response"
                  body: "Different Message!"
