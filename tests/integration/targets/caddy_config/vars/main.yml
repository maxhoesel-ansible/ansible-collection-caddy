original_config:
  apps:
    http:
      servers:
        example:
          listen:
            - ":80"
          routes:
            - handle:
              - handler: "static_response"
                body: "Hello World!"

expected_config:
  apps:
    http:
      servers:
        example:
          listen:
            - ":3456"
          routes:
            - handle:
              - handler: "static_response"
                body: "Different Message!"
